# Research

Current state ownership is spread across runner fields, allocation, input
preparation, rejected-prefix replay, Full Graph padding, cleanup, and benchmark
attribute access. The manager must preserve those external observations while
making slab/slot mutation independently testable.

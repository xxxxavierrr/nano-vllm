# Research

The scheduler protocol is already generic. Runner coupling is concentrated in
MTP slot mapping, proposal metadata/tensors, recursive `k` steps, target
embedding/logit use, and direct calls to the greedy acceptance function.
